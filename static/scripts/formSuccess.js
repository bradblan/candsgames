var strQueryString="";
	var hasQueryString = document.URL.indexOf('?');
	if (hasQueryString != -1)
	{
		if (strQueryString = "message=show")
		{
			alert('Thank you for sending us a quote. We will be contacting you soon for additional details.');
		}
	}